<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="attribute_style">
        <item name="android:padding">5dp</item>
    </style>

    <style name="navBottomRB">
        <item name="android:padding">5dp</item>
        <item name="android:drawablePadding">5dp</item>
    </style>

    <style name="dialog" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">false</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>
    <!--
    <style name="CardView.layoutStyle">
        <item name="app:cardBackgroundColor">@color/cardview_light_background</item>
        <item name="app:cardCornerRadius">5dp</item>
        <item name="app:cardElevation">5dp</item>
        <item name="app:cardPreventCornerOverlap">true</item>
        <item name="app:cardUseCompatPadding">true</item>
        <item name=" app:contentPadding">10dp</item>
        <item name="app:layout_collapseMode">parallax</item>
        <item name="app:layout_collapseParallaxMultiplier">@android:integer/config_mediumAnimTime
        </item>
        <item name="app:theme">@style/AppTheme</item>
    </style>-->
</resources>
